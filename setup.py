from setuptools import setup, find_packages

setup(
    name='PDFExtract',
    description='This library contains the utils for extracting information from PDFs',
    version='0.1.0',
    packages=find_packages(),
    author='TZZ'
)
